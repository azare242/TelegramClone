import { createTheme } from "@mui/material"
import { blue, green, lightBlue, lightGreen } from "@mui/material/colors"


export const theme = createTheme({
    palette: {
        primary: green,
        secondary: lightGreen,
    },
})